import 'package:flutter/material.dart';

class ErrerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('路由错误页面'),
      ),
      body: Container(
        color: Colors.redAccent,
      ),
    );
  }
}
